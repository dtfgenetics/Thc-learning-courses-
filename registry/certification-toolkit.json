{
  "id": "certification-toolkit",
  "version": "1.1.0",
  "description": "Orchestrates certification course validation and selects the next production-readiness task without duplicating active work.",
  "entrypoint": "scripts/certification-toolkit.mjs",
  "worker": {
    "entrypoint": "scripts/certification-production-worker.mjs",
    "test": "scripts/test-certification-production-worker.mjs",
    "readinessSource": "registry/system-readiness.json",
    "activeWorkInput": "optional JSON array supplied with --active-work=path",
    "selectionPolicy": [
      "resume an open task that already owns the unresolved gate",
      "otherwise select the highest-priority unresolved readiness gate",
      "only recommend a new work branch when no active owner exists",
      "when no blockers remain, run the release gate"
    ]
  },
  "modes": {
    "author": { "purpose": "Validate authored curriculum and knowledge content before review.", "groups": ["content"] },
    "exam": { "purpose": "Validate curriculum plus assessment and pilot readiness.", "groups": ["content", "assessment"] },
    "certify": { "purpose": "Validate the full certification evidence chain before learner delivery.", "groups": ["content", "assessment", "credential", "review"] },
    "learner": { "purpose": "Validate learner-facing academy behavior, progress, enrollment, and accessibility.", "groups": ["content", "learner"] },
    "platform": { "purpose": "Validate API, runtime, persistence, database, security, and operations controls.", "groups": ["platform"] },
    "release": { "purpose": "Run the complete production release gate including staging and production readiness.", "groups": ["content", "assessment", "credential", "review", "learner", "platform", "release"] },
    "full": { "purpose": "Run every registered certification toolkit group.", "groups": ["content", "assessment", "credential", "review", "learner", "platform", "release"] }
  },
  "behavior": {
    "defaultMode": "full",
    "failFast": true,
    "continueFlag": "--continue",
    "jsonFlag": "--json",
    "listFlag": "--list"
  }
}
