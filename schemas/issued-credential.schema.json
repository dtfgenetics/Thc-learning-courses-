{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://academy.dtfseeds.com/schemas/issued-credential.schema.json",
  "title": "THC Academy Issued Credential Record",
  "type": "object",
  "required": ["id", "credentialDefinition", "credentialVersion", "subjectId", "status", "issuedAt", "verificationId", "assessmentEvidence", "issuer", "integrityHash"],
  "properties": {
    "id": {"type": "string", "pattern": "^(TEST-)?CREDENTIAL-[A-Z0-9-]+$"},
    "credentialDefinition": {"type": "string", "pattern": "^CRED-[A-Z0-9-]+$"},
    "credentialVersion": {"type": "string", "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$"},
    "subjectId": {"type": "string", "minLength": 3},
    "status": {"enum": ["test-issued", "issued", "valid", "suspended", "superseded", "expired", "revoked"]},
    "issuedAt": {"type": "string", "format": "date-time"},
    "expiresAt": {"type": ["string", "null"], "format": "date-time"},
    "verificationId": {"type": "string", "minLength": 8},
    "assessmentEvidence": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["assessmentId", "scorePercent", "status"],
        "properties": {
          "assessmentId": {"type": "string", "pattern": "^ASSESS-[A-Z0-9-]+$"},
          "scorePercent": {"type": "number", "minimum": 0, "maximum": 100},
          "status": {"const": "passed"}
        },
        "additionalProperties": false
      }
    },
    "performanceEvidence": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["assessmentId", "status", "criticalErrorCount", "evidenceVerified"],
        "properties": {
          "assessmentId": {"type": "string", "pattern": "^(PRACTICAL|CAPSTONE)-[A-Z0-9-]+$"},
          "scorePercent": {"type": ["number", "null"], "minimum": 0, "maximum": 100},
          "status": {"const": "passed"},
          "criticalErrorCount": {"type": "integer", "minimum": 0},
          "evidenceVerified": {"type": "boolean"}
        },
        "additionalProperties": false
      }
    },
    "portfolioEvidence": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["artifactId", "status"],
        "properties": {
          "artifactId": {"type": "string", "minLength": 3},
          "status": {"enum": ["accepted", "verified", "complete"]}
        },
        "additionalProperties": false
      }
    },
    "publicEvidenceSummary": {
      "type": "object",
      "required": ["writtenAssessments", "performanceAssessments", "portfolioArtifacts"],
      "properties": {
        "writtenAssessments": {"type": "integer", "minimum": 0},
        "performanceAssessments": {"type": "integer", "minimum": 0},
        "portfolioArtifacts": {"type": "integer", "minimum": 0}
      },
      "additionalProperties": false
    },
    "issuer": {
      "type": "object",
      "required": ["name", "url"],
      "properties": {
        "name": {"type": "string"},
        "url": {"type": "string"}
      },
      "additionalProperties": false
    },
    "disclaimer": {"type": "string"},
    "integrityHash": {"type": "string", "minLength": 32}
  },
  "additionalProperties": false
}
