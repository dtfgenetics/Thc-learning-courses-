import fs from 'node:fs';
import path from 'node:path';
const root=process.cwd();
const read=(p)=>JSON.parse(fs.readFileSync(path.join(root,p),'utf8'));
const write=(p,o)=>{const f=path.join(root,p);fs.mkdirSync(path.dirname(f),{recursive:true});fs.writeFileSync(f,`${JSON.stringify(o,null,2)}\n`);};

const assessmentPath='content/assessments/ASSESS-CULT-TECH-II-CREDENTIAL-001.json';
const assessment=read(assessmentPath);
assessment.extensions={...(assessment.extensions??{}),developmentBlueprintOnly:true,operationalUseAuthorized:false,publicRepositoryItemsMayBeUsedForOperationalCredentialForms:false,secureOperationalItemBankRequired:true,privateOperationalAssessmentStoreRequired:true,secureEquivalentFormsRequired:true};
write(assessmentPath,assessment);

write('registry/technician-ii-release-evidence.json',{
 id:'RELEASE-EVIDENCE-TECH2-001',version:'1.0.0',status:'draft',credentialProgram:'CREDPROG-CULT-TECH-II-001',releaseReady:false,
 evidencePolicy:'Only completed human review, employer/job-task validation, pilot/performance evidence, evaluator calibration, formal standard-setting evidence, approved security/privacy controls and explicit release approval may advance a release gate. Finished curriculum files, passing CI, generated development forms, synthetic test records or course completion alone must never authorize credential issuance.',
 structuralState:{curriculumSequenceBuilt:true,requiredCourses:8,practicalsMapped:7,capstoneMapped:true,integratedLabBuilt:true,publicCredentialBlueprintOperationalUseAuthorized:false},
 gates:{humanTechnicalReview:'not-started',humanAssessmentReview:'not-started',renderedAccessibilityReview:'not-started',jobTaskAnalysisValidation:'draft',smeEmployerValidation:'not-started',controlledPilotEvidence:'not-started',practicalValidation:'not-started',capstoneValidation:'not-started',evaluatorCalibration:'not-started',interRaterEvidence:'not-started',blueprintWeightsFinalization:'not-started',secureOperationalItemBank:'not-built',secureAssessmentStore:'not-approved',equivalentSecureForms:'not-built',formalStandardSetting:'not-started',candidateEvidenceRetentionPrivacyPolicy:'not-approved',credentialIssuanceWorkflowApproval:'not-approved',finalProgramReleaseApproval:'not-requested'},
 requiredEvidence:['versioned human technical/curriculum review approval','versioned human assessment review approval','rendered accessibility review evidence','validated Technician II job-task analysis','SME/employer validation evidence','controlled learner/item pilot evidence','validated Practical A-G performance evidence','validated Senior Technician Diagnostic Shift capstone evidence','evaluator calibration and inter-rater evidence','final written credential blueprint weights','new secure operational item bank in an approved private assessment store','equivalent secure credential forms','formal standard-setting study and approval','approved candidate-evidence retention/privacy policy','approved credential issuance workflow','explicit versioned final Technician II program release approval'],
 prohibitedShortcuts:['Do not treat green CI, complete course files, or the integrated lab build as certification approval.','Do not use public ITEM-TECH2 development items as a secure operational credential bank.','Do not create synthetic human review, pilot, employer-validation, calibration, standard-setting or approval evidence to close gates.','Do not expose secure operational answer keys, selected forms or delivery material in the public repository.','Do not average away a failed Technician II practical or capstone critical error.','Do not issue the Technician II credential while any required release gate remains unresolved.']
});

const reporter=`import fs from 'node:fs';\nimport path from 'node:path';\nconst root=process.cwd();\nconst args=new Set(process.argv.slice(2));\nconst read=(p)=>JSON.parse(fs.readFileSync(path.join(root,p),'utf8'));\nconst exists=(p)=>fs.existsSync(path.join(root,p));\nconst program=read('content/credential-programs/CREDPROG-CULT-TECH-II-001.json');\nconst evidence=read('registry/technician-ii-release-evidence.json');\nconst labPlan=read('registry/technician-ii-integrated-lab-plan.json');\nconst programRegistry=read('content/credential-programs/registry.json');\nconst malformed=[]; const blockers=[];\nconst addMalformed=(reason,detail)=>malformed.push({reason,detail}); const addBlocker=(reason,detail)=>blockers.push({reason,detail});\nif(program.id!=='CREDPROG-CULT-TECH-II-001')addMalformed('program-id-mismatch',String(program.id));\nif(!Array.isArray(program.requiredCourses)||program.requiredCourses.length!==8)addMalformed('required-course-contract-invalid','Technician II must resolve exactly eight required course objects.');\nfor(const id of program.requiredCourses??[]){if(!exists('content/courses/'+id+'.json'))addMalformed('missing-required-course',id);}\nfor(const id of program.competencies??[]){if(!exists('content/competencies/'+id+'.json'))addMalformed('missing-program-competency',id);}\nconst assessmentId=program.assessmentModel?.credentialAssessment;\nif(!assessmentId)addMalformed('credential-assessment-id-missing','Program must identify its credential assessment blueprint.'); else if(!exists('content/assessments/'+assessmentId+'.json'))addMalformed('credential-assessment-object-missing',assessmentId);\nlet assessment=null; if(assessmentId&&exists('content/assessments/'+assessmentId+'.json')){assessment=read('content/assessments/'+assessmentId+'.json'); if(assessment.id!==assessmentId)addMalformed('credential-assessment-id-mismatch',assessment.id+' != '+assessmentId); if(assessment.purpose!=='credential')addMalformed('credential-assessment-purpose-invalid',String(assessment.purpose)); if(assessment.status!=='draft')addMalformed('public-credential-blueprint-status-unsafe','Public Technician II credential blueprint must remain draft until secure operational assessment is approved.'); if((assessment.items??[]).length!==0)addMalformed('public-operational-item-exposure','Public Technician II credential blueprint must contain zero selected operational secure items.'); if(assessment.extensions?.operationalUseAuthorized!==false)addMalformed('public-blueprint-authorization-unsafe','Public Technician II blueprint must explicitly deny operational use.'); if(assessment.extensions?.publicRepositoryItemsMayBeUsedForOperationalCredentialForms!==false)addMalformed('public-bank-boundary-unsafe','Public Technician II development items must be barred from operational credential forms.'); if(assessment.extensions?.secureOperationalItemBankRequired!==true)addMalformed('secure-bank-requirement-missing','Technician II blueprint must require a separate secure operational bank.'); if(assessment.extensions?.privateOperationalAssessmentStoreRequired!==true)addMalformed('private-store-requirement-missing','Technician II blueprint must require a private operational assessment store.');}\nconst registryEntry=(programRegistry.programs??[]).find(r=>r.id===program.id); if(!registryEntry)addMalformed('program-registry-entry-missing',program.id); else {if(registryEntry.credentialAssessment!==assessmentId)addMalformed('program-registry-assessment-mismatch',String(registryEntry.credentialAssessment)+' != '+assessmentId); if(registryEntry.capstone!==program.assessmentModel?.capstone)addMalformed('program-registry-capstone-mismatch',String(registryEntry.capstone)+' != '+String(program.assessmentModel?.capstone));}\nif(evidence.credentialProgram!==program.id)addMalformed('release-evidence-program-mismatch',String(evidence.credentialProgram)); if(evidence.releaseReady!==false&&evidence.status==='draft')addMalformed('draft-evidence-release-flag-unsafe','Draft Technician II release evidence cannot assert releaseReady=true.'); if(labPlan.credentialProgram!==program.id)addMalformed('lab-plan-program-mismatch',String(labPlan.credentialProgram)); if((labPlan.practicals??[]).length!==7)addMalformed('practical-plan-count-invalid','Expected 7 practicals, found '+String((labPlan.practicals??[]).length)); for(const p of labPlan.practicals??[]){if(!exists(p.document))addMalformed('practical-document-missing',String(p.document)); if(p.totalPoints!==100||p.targetPassPoints!==80||p.noCriticalErrors!==true)addMalformed('practical-scoring-contract-invalid',p.id);}\nif(!exists(labPlan.capstone?.document??''))addMalformed('capstone-document-missing',String(labPlan.capstone?.document??'none')); if(labPlan.capstone?.id!==program.assessmentModel?.capstone)addMalformed('capstone-id-mismatch',String(labPlan.capstone?.id)+' != '+String(program.assessmentModel?.capstone)); if(labPlan.capstone?.totalPoints!==100||labPlan.capstone?.developmentTargetPassPoints!==80||labPlan.capstone?.noCriticalErrors!==true)addMalformed('capstone-scoring-contract-invalid','Expected 100 points, 80 minimum, no critical errors.'); if(labPlan.completionLogic?.compensatoryAveragingAllowed!==false)addMalformed('compensatory-averaging-unsafe','Technician II practical/capstone completion must be non-compensatory.'); if(labPlan.completionLogic?.readinessAssessmentIsCredentialEvidence!==false)addMalformed('readiness-evidence-boundary-unsafe','Course 208 readiness check must not be credential evidence.');\nif(!['approved','published'].includes(program.status))addBlocker('program-status-not-approved','status='+program.status); for(const [field,value] of Object.entries(program.validation??{})){if(value!=='validated')addBlocker('program-validation-'+field+'-incomplete',field+'='+value);} if(program.assessmentModel?.standardSettingStatus!=='validated')addBlocker('standard-setting-incomplete','status='+String(program.assessmentModel?.standardSettingStatus));\nconst required={humanTechnicalReview:'approved',humanAssessmentReview:'approved',renderedAccessibilityReview:'approved',jobTaskAnalysisValidation:'validated',smeEmployerValidation:'validated',controlledPilotEvidence:'accepted',practicalValidation:'validated',capstoneValidation:'validated',evaluatorCalibration:'complete',interRaterEvidence:'accepted',blueprintWeightsFinalization:'finalized',secureOperationalItemBank:'approved-private-operational',secureAssessmentStore:'approved',equivalentSecureForms:'approved',formalStandardSetting:'validated',candidateEvidenceRetentionPrivacyPolicy:'approved',credentialIssuanceWorkflowApproval:'approved',finalProgramReleaseApproval:'approved'};\nfor(const [gate,need] of Object.entries(required)){const actual=evidence.gates?.[gate]; if(actual===undefined)addMalformed('release-gate-missing',gate); else if(actual!==need)addBlocker('release-gate-'+gate+'-incomplete',String(actual)+'; required='+need);}\nif(labPlan.capstone?.secureCredentialFormApproved!==true)addBlocker('secure-capstone-form-not-approved','approved='+String(labPlan.capstone?.secureCredentialFormApproved)); if((labPlan.practicals??[]).some(r=>r.status!=='validated'))addBlocker('practical-set-not-validated','At least one Practical A-G remains development/unvalidated.'); if(labPlan.evaluatorControls?.interRaterAgreementTarget==='to-be-set-from-pilot-evidence')addBlocker('inter-rater-target-not-finalized',labPlan.evaluatorControls.interRaterAgreementTarget);\nconst releaseReady=malformed.length===0&&blockers.length===0; if(evidence.releaseReady!==releaseReady)addMalformed('committed-release-ready-flag-mismatch','evidence.releaseReady='+String(evidence.releaseReady)+'; computed='+String(releaseReady));\nconst result={credentialProgram:program.id,releaseReady:malformed.length===0&&blockers.length===0,structureValid:malformed.length===0,malformed,blockers,summary:{requiredCourses:program.requiredCourses?.length??0,practicals:labPlan.practicals?.length??0,capstone:labPlan.capstone?.id??null,credentialAssessment:assessmentId??null,publicCredentialAssessmentItems:assessment?.items?.length??null,integratedLab:labPlan.courseId??null,unresolvedBlockers:blockers.length,structuralErrors:malformed.length}};\nif(args.has('--human')){console.log('Technician II release readiness: '+(result.releaseReady?'READY':'BLOCKED'));console.log('Structure valid: '+String(result.structureValid));console.log('Credential assessment: '+String(result.summary.credentialAssessment)+'; public operational items: '+String(result.summary.publicCredentialAssessmentItems));console.log('Required courses: '+String(result.summary.requiredCourses)+'; practicals: '+String(result.summary.practicals)+'; capstone: '+String(result.summary.capstone));if(malformed.length){console.log('Structural errors:');for(const r of malformed)console.log('- '+r.reason+': '+r.detail);}if(blockers.length){console.log('Release blockers:');for(const r of blockers)console.log('- '+r.reason+': '+r.detail);}}else console.log(JSON.stringify(result,null,2));\nif(args.has('--check-structure')&&malformed.length)process.exit(1); if(args.has('--require-ready')&&!result.releaseReady)process.exit(2);\n`;
fs.writeFileSync(path.join(root,'scripts/report-tech2-release-readiness.mjs'),reporter);

const test=`import assert from 'node:assert/strict';\nimport fs from 'node:fs';\nimport {spawnSync} from 'node:child_process';\nconst run=spawnSync(process.execPath,['scripts/report-tech2-release-readiness.mjs'],{encoding:'utf8'}); assert.equal(run.status,0,run.stderr||'Technician II release-readiness reporter should execute'); const result=JSON.parse(run.stdout);\nassert.equal(result.structureValid,true,'Technician II release-readiness structure must resolve cleanly: '+JSON.stringify(result.malformed)); assert.equal(result.releaseReady,false,'Technician II must remain release-blocked until real validation and release evidence exist'); assert.equal(result.summary.requiredCourses,8); assert.equal(result.summary.practicals,7); assert.equal(result.summary.capstone,'CAPSTONE-TECH2-SENIOR-TECHNICIAN-DIAGNOSTIC-SHIFT'); assert.equal(result.summary.credentialAssessment,'ASSESS-CULT-TECH-II-CREDENTIAL-001'); assert.equal(result.summary.publicCredentialAssessmentItems,0); assert.equal(result.summary.integratedLab,'COURSE-LH-TECH2-008');\nconst reasons=new Set(result.blockers.map(r=>r.reason)); for(const reason of ['program-status-not-approved','program-validation-jobTaskAnalysis-incomplete','program-validation-smeEmployerValidation-incomplete','program-validation-assessmentReview-incomplete','program-validation-accessibilityReview-incomplete','standard-setting-incomplete','release-gate-humanTechnicalReview-incomplete','release-gate-humanAssessmentReview-incomplete','release-gate-renderedAccessibilityReview-incomplete','release-gate-controlledPilotEvidence-incomplete','release-gate-practicalValidation-incomplete','release-gate-capstoneValidation-incomplete','release-gate-evaluatorCalibration-incomplete','release-gate-interRaterEvidence-incomplete','release-gate-blueprintWeightsFinalization-incomplete','release-gate-secureOperationalItemBank-incomplete','release-gate-secureAssessmentStore-incomplete','release-gate-equivalentSecureForms-incomplete','release-gate-formalStandardSetting-incomplete','release-gate-candidateEvidenceRetentionPrivacyPolicy-incomplete','release-gate-credentialIssuanceWorkflowApproval-incomplete','release-gate-finalProgramReleaseApproval-incomplete','secure-capstone-form-not-approved','practical-set-not-validated','inter-rater-target-not-finalized'])assert.ok(reasons.has(reason),'missing expected fail-closed blocker '+reason);\nconst assessment=JSON.parse(fs.readFileSync('content/assessments/ASSESS-CULT-TECH-II-CREDENTIAL-001.json','utf8')); assert.equal(assessment.status,'draft'); assert.equal(assessment.purpose,'credential'); assert.deepEqual(assessment.items,[]); assert.equal(assessment.extensions?.operationalUseAuthorized,false); assert.equal(assessment.extensions?.publicRepositoryItemsMayBeUsedForOperationalCredentialForms,false); assert.equal(assessment.extensions?.secureOperationalItemBankRequired,true); assert.equal(assessment.extensions?.privateOperationalAssessmentStoreRequired,true);\nconst requireReady=spawnSync(process.execPath,['scripts/report-tech2-release-readiness.mjs','--require-ready'],{encoding:'utf8'}); assert.equal(requireReady.status,2,'--require-ready must fail closed while Technician II release blockers remain');\nconsole.log('Technician II release readiness passed: curriculum structure resolves and issuance remains fail-closed behind '+String(result.blockers.length)+' explicit blocker(s).');\n`;
fs.writeFileSync(path.join(root,'scripts/test-tech2-release-readiness.mjs'),test);

const pkg=read('package.json'); pkg.scripts['tech2:release-readiness']='node scripts/report-tech2-release-readiness.mjs --human'; pkg.scripts['tech2:release-readiness:test']='node scripts/test-tech2-release-readiness.mjs'; if(!pkg.scripts.test.includes('npm run tech2:release-readiness:test'))pkg.scripts.test=pkg.scripts.test.replace('npm run tech2:course8:test','npm run tech2:course8:test && npm run tech2:release-readiness:test'); write('package.json',pkg);

fs.writeFileSync(path.join(root,'docs/academy-v2/TECH2_RELEASE_READINESS_MILESTONE.md'),`# Technician II Release-Readiness Milestone\n\nThe eight-course Technician II curriculum, seven-practical performance set and Senior Technician Diagnostic Shift capstone now resolve structurally. This milestone deliberately does **not** mark the credential ready for issuance.\n\nA dedicated release-evidence ledger and deterministic readiness reporter now separate curriculum construction from credential authorization. The public Technician II credential assessment is explicitly development-only, contains no selected operational secure items, denies operational use of public repository items, and requires a separate private operational item bank/store and equivalent secure forms.\n\nThe reporter must remain BLOCKED while human technical/assessment/accessibility review, JTA and SME/employer validation, pilot evidence, Practical A-G validation, capstone validation, evaluator calibration/inter-rater evidence, final blueprint weights, secure bank/store/forms, formal standard setting, candidate-evidence retention/privacy policy, issuance-workflow approval and final program release approval remain incomplete.\n\nGreen CI, built courses, synthetic test records or the public development item bank are prohibited from closing these gates.\n`);
console.log('Built Technician II fail-closed release-readiness governance.');
