import assert from 'node:assert/strict';import fs from 'node:fs';const s=JSON.parse(fs.readFileSync('visuals/COURSE3-PRODUCTION-MASTER-BATCH1-BUILD-SPEC.json','utf8'));const q=JSON.parse(fs.readFileSync('visuals/COURSE3-PRODUCTION-MASTER-BATCH1-QA.json','utf8'));assert.equal(s.assets.length,4);assert.equal(q.requiredRebuilds.length,4);assert.equal(q.referenceBoard.releaseApproved,false);for(const a of s.assets){assert.match(a.fileName,/\.png$/);assert.ok(a.alt.length>80);assert.ok(!JSON.stringify(a).match(/seedling|vegetative|flowering week|recommended range/i));}console.log('Course 3 production master batch 1 build spec passed.');
